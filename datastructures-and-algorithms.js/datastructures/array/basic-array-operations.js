const arr = [10, 28,7,4,8,8]
arr.push(10) // adds element to last
arr.pop() // removes element from last  
arr.unshift(150) // adds element on first
arr.shift() // removes element from first
console.log(arr)